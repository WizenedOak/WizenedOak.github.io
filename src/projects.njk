---
title: My Projects
layout: layout.njk
---

<section class="projects_intro">
    <h2>Personal Projects</h2>
    <p>
        A collection of some of the systems and applications I've built, focused on usability and performance.
    </p>
</section>

<section class="projects_card_container">
    {% for project in projects %}
        <a href="{{ project.link }}" target="_blank"
           class="projects_card {% if loop.index % 2 == 0 %}reverse{% endif %}">
            
            <div class="projects_image_wrapper">
                <img src="{{ project.img }}" alt="{{ project.alt }}" loading="lazy">
            </div>

            <div class="projects_card_content">
                <h3>{{ project.title }}</h3>

                <div class="tech_stack">
                    {% for tech in project.tech %}
                        <span class="tech_pill">{{ tech }}</span>
                    {% endfor %}
                </div>

                <p>{{ project.description }}</p>

                <span class="view_project">View Project →</span>
            </div>

        </a>
    {% endfor %}
</section>
